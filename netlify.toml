
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  publish = "."

[context.production.environment]
  # These are placeholders; Netlify will use the values defined in the UI dashboard.
  SUPABASE_URL = "SET_IN_NETLIFY_UI"
  SUPABASE_ANON_KEY = "SET_IN_NETLIFY_UI"
  API_KEY = "SET_IN_NETLIFY_UI"
